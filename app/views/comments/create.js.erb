<% if @comment.valid? %>
  var $postCmnt = $('.js-post-comments-count');

  $('.js-comments').prepend("<%= escape_javascript(render(@comment)) %>");
    if ($postCmnt.length > 0){
      $('.js-post-comments-count').html("<%= pluralize(@post.comments.count, 'comment') %>");
    }
  $('.new-comment .errors').empty()
  $('.new-comment textarea').val("");
<% else %>
  $('.new-comment .errors').html("<%= escape_javascript(render partial: 'comments/errors', locals: { comment: @comment }) %>");
<% end %>
