%h1 Topics
.row
  .col-md-8
    - @topics.each do |topic|
      .media
        .media-body
          %h4{:class => "media-heading"}= link_to markdown(topic.name), topic
          %small= markdown(topic.description)
  .col-md-4
    - if policy(Topic.new).create?
      = link_to "New Topic", new_topic_path, class: 'btn btn-success'