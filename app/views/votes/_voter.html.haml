- if policy(Vote.new).create?
  .pull-left.vote-arrows
    %div= link_to ' ', post_up_vote_path(post), class: "glyphicon glyphicon-chevron-up #{'voted' if (current_user.voted(post) && current_user.voted(post).up_vote?)}", method: :post
    %div
      %strong= post.points
    %div= link_to ' ', post_down_vote_path(post), class: "glyphicon glyphicon-chevron-down #{'voted' if (current_user.voted(post) && current_user.voted(post).down_vote?)}", method: :post